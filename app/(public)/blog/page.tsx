import Link from 'next/link';
import type { Metadata } from 'next';
import { ArrowRight } from 'lucide-react';
import { blogPosts } from '@/lib/content/site';
export const metadata: Metadata = { title: 'Business Funding Blog | Elite Funding Solutions', description: 'Business funding insights, underwriting preparation tips, and capital strategy articles from Elite Funding Solutions.', alternates: { canonical: '/blog' } };
export default function BlogPage() { return <main className="section bg-[#F8F9FB]"><div className="container-page"><div className="mb-12 max-w-3xl"><p className="eyebrow mb-3">Blog</p><h1 className="text-4xl font-semibold tracking-tight text-[#0A1628] md:text-6xl">Business funding insights.</h1><p className="mt-5 text-lg leading-8 text-[#5A6A85]">Actionable guidance for preparing applications, comparing funding structures, and reducing underwriting friction.</p></div><div className="grid gap-5">{blogPosts.map((post) => <article key={post.slug} className="premium-card p-6 md:p-8"><p className="mb-3 text-xs font-bold uppercase tracking-[0.16em] text-[#8A6A22]">{post.minutes} min read · Updated {post.updated}</p><h2 className="text-2xl font-semibold tracking-tight text-[#0A1628]"><Link href={`/blog/${post.slug}`} className="hover:text-[#8A6A22]">{post.title}</Link></h2><p className="mt-3 leading-7 text-[#5A6A85]">{post.description}</p><Link href={`/blog/${post.slug}`} className="mt-5 inline-flex items-center gap-2 font-semibold text-[#0A1628]">Read article <ArrowRight className="h-4 w-4" /></Link></article>)}</div></div></main>; }
